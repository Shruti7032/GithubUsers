"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCatholicDailyReadings = void 0;
const puppeteer_1 = __importDefault(require("puppeteer"));
const date_fns_1 = require("date-fns");
const parser_1 = require("./parser");
function getCatholicDailyReadings(date) {
    return __awaiter(this, void 0, void 0, function* () {
        // get page source
        date = date !== null && date !== void 0 ? date : new Date();
        console.log(date);
        const formattedDate = (0, date_fns_1.format)(date, "MMddyy");
        console.log(formattedDate);
        const url = `https://bible.usccb.org/bible/readings/${formattedDate}.cfm`;
        // logger.info("launching browser...");
        console.log("launching browser");
        const browser = yield puppeteer_1.default.launch({
            headless: true,
            args: [
                "--disable-gpu",
                "--disable-dev-shm-usage",
                "--disable-setuid-sandbox",
                "--no-first-run",
                "--no-sandbox",
                "--no-zygote",
                "--single-process",
                "--proxy-server='direct://'",
                "--proxy-bypass-list=*",
                "--deterministic-fetch",
                "--font-render-hinting=medium",
            ],
        });
        //   logger.info("starting page...");
        console.log("starting page");
        const page = yield browser.newPage();
        yield page.setUserAgent("Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/78.0.3904.108 Safari/537.36");
        yield page.emulateTimezone("America/Detroit");
        // load page
        console.log("loading page");
        yield page.goto(url, { waitUntil: "networkidle0" });
        // find lectionary header by class 'b-lectionary'
        console.log("getting lectionary");
        const lectionaryNode = yield page.$(".b-lectionary");
        if (!lectionaryNode) {
            throw new Error("couldn't parse lectionary");
        }
        // find readings by class 'b-verse'
        console.log("getting readings");
        const readingNodes = yield page.$$(".b-verse");
        const readings = yield getDailyReadingsFromNodes(lectionaryNode, readingNodes);
        return readings;
    });
}
exports.getCatholicDailyReadings = getCatholicDailyReadings;
function getDailyReadingsFromNodes(lectionaryNode, readingNodes) {
    return __awaiter(this, void 0, void 0, function* () {
        const readingPromises = readingNodes.map(getReadingsFromNode);
        const promises = [getLectionaryHeaderFromNode(lectionaryNode), getLectionaryNumberFromNode(lectionaryNode), ...readingPromises];
        const [header, lectionary, ...readings] = yield Promise.all(promises);
        const flatReadings = readings.flat();
        return {
            header,
            lectionary,
            readings: flatReadings,
        };
    });
}
function getLectionaryHeaderFromNode(node) {
    var _a;
    return __awaiter(this, void 0, void 0, function* () {
        return (_a = (yield (node === null || node === void 0 ? void 0 : node.$eval("div.innerblock h2", (el) => { var _a; return (_a = el.textContent) === null || _a === void 0 ? void 0 : _a.trim(); })))) !== null && _a !== void 0 ? _a : "";
    });
}
function getLectionaryNumberFromNode(node) {
    var _a;
    return __awaiter(this, void 0, void 0, function* () {
        const lectionaryText = (_a = (yield (node === null || node === void 0 ? void 0 : node.$eval("div.innerblock p", (el) => { var _a; return (_a = el.textContent) === null || _a === void 0 ? void 0 : _a.trim(); })))) !== null && _a !== void 0 ? _a : "";
        const match = lectionaryText.match(/[0-9]+/gm);
        if (!match)
            throw new Error("could not parse lectionary number");
        return +match[0];
    });
}
function getReadingsFromNode(node) {
    return __awaiter(this, void 0, void 0, function* () {
        // build promises
        const promises = [node === null || node === void 0 ? void 0 : node.$eval("div.content-header h3.name", (el) => { var _a; return (_a = el.textContent) === null || _a === void 0 ? void 0 : _a.trim(); })];
        const refNode = node === null || node === void 0 ? void 0 : node.$("div.address a");
        if (!!refNode) {
            const refPromise = refNode.then((a) => a === null || a === void 0 ? void 0 : a.evaluate((el) => { var _a; return (_a = el.textContent) === null || _a === void 0 ? void 0 : _a.trim(); }));
            promises.push(refPromise);
        }
        promises.push(node === null || node === void 0 ? void 0 : node.$eval("div.content-body", (el) => el.innerHTML.replace(/(^&nbsp;)|((?<=\&nbsp;)&nbsp;)|((?<=\s)&nbsp;)/gm, "").replace(/&nbsp;/g, " ")));
        // resolve promises
        const [header, reference, content] = yield Promise.all(promises);
        return (0, parser_1.parseReadings)(header, reference, content);
    });
}
//# sourceMappingURL=index.js.map