export declare function getCatholicDailyReadings(date?: Date): Promise<DailyReadings>;
export interface DailyReadings {
    header: string;
    lectionary: number;
    readings: Reading[];
}
export interface Reading {
    header: string;
    reference: string;
    rawText: string;
    nonFormattedText: string;
    formattedText: string;
}
